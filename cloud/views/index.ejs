<% include header %>

    <div class="container">
      <div class="jumbotron">
        <img src="images/chineselink.png"></img>
        <form class="form-inline" style="padding-top:30px;" action="/generate" method="post">
          <div class="form-group">
            <label><%= __("Chinese URL") %></label>
            <input type="text" class="form-control" name="url" value="http://" size="50">
          </div>
          <button type="submit" class="btn btn-default"><%= __("Generate") %></button>
        </form>
      </div>
    </div>

<% include footer %>

